apiVersion: v1
kind: Pod
metadata:
  name: multitool-pod
spec:
  containers:
    - name: multitool
      image: praqma/network-multitool
      command: ["/bin/sh", "-c", "while true; do sleep 3600; done"]
  restartPolicy: Never
